# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel

# Firebase Admin SDK service account key — full database access, never commit
import-tool/serviceAccountKey.json
serviceAccountKey.json

# Env files with secrets (.env.local already caught by *.local above, .env explicit here)
.env

# ข้อมูลนักเรียนจริง (ชื่อ-นามสกุล + รหัสนักเรียน) ห้าม push ขึ้น GitHub
# ใช้ไฟล์ .example เป็นตัวอย่างใน repo แทน
supabase/migrations/06_seed_real.sql

# รายชื่อสำหรับ import — มีชื่อจริงและรหัสผ่าน (เลขบัตร/รหัสนักเรียน)
# repo นี้เป็น public ถ้าหลุดคือใครก็ล็อกอินแทนนักเรียนได้
import-tool/students.csv
import-tool/staff.csv

# ไฟล์รวมที่สร้างไว้รันครั้งเดียว — มีเนื้อหาของ 06_seed_real.sql (ชื่อจริง) อยู่ข้างใน
supabase/migrations/RUN_ME_ALL.sql
supabase/migrations/RUN_08_09.sql

# บัญชีบุคลากรจริง — มีชื่อผู้ใช้ที่ใช้ล็อกอินจริง ไม่ควรขึ้น repo สาธารณะ
supabase/migrations/08_seed_staff.sql

.vercel
.env*

# ผลตรวจของเครื่องมือ design review (impeccable) ไม่ใช่ซอร์สโค้ดของโปรเจกต์
.impeccable/
