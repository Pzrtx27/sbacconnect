/* ข้อความสำหรับสถานะ "ฟีเจอร์นี้ยังไม่ถูกเปิดใช้งานในฐานข้อมูล"
 *
 * ทำไมต้องมีที่เดียว: ของเดิมแต่ละหน้าพิมพ์ข้อความเอง แล้วกลายเป็นสองภาษาปนกัน
 *   "ยังไม่ได้ติดตั้งระบบประวัติ (รัน 26_wallet_history.sql บน Supabase ก่อน)"
 *   "ระบบคะแนนยังไม่ถูกเปิดใช้งาน กรุณาแจ้งฝ่ายวิชาการ"
 * แบบแรกเป็นภาษาของคนเขียนโค้ด แต่คนอ่านคือนักเรียน ม.ปลาย ที่เปิดดูยอดเงินตัวเอง
 * เขาทำอะไรกับชื่อไฟล์ .sql ไม่ได้เลย และไม่รู้ด้วยซ้ำว่า Supabase คืออะไร
 *
 * ข้อความที่ใช้ได้จริงต้องตอบสามข้อ: อะไรใช้ไม่ได้ / เพราะอะไร / แล้วต้องทำยังไงต่อ
 * ข้อสามคือข้อที่หายไปทั้งหมดในของเดิม — บอกไปเลยว่าต้องบอกใคร
 * ส่วนชื่อไฟล์ยังมีประโยชน์กับคนที่แก้ได้ จึงย้ายไปที่ console ไม่ใช่ลบทิ้ง
 */

/** ทีมที่ผู้ใช้ต้องไปบอก แยกตามว่าใครเป็นคนแก้เรื่องนั้นได้จริง */
export const CONTACT = {
  academic: 'ฝ่ายวิชาการ',
  finance: 'ฝ่ายการเงิน',
  registrar: 'ฝ่ายทะเบียน',
  admin: 'ผู้ดูแลระบบ',
};

/** ข้อความที่ผู้ใช้เห็น — ชื่อฟีเจอร์เป็นคำที่เขาเพิ่งกดเข้ามา ไม่ใช่ชื่อตารางใน DB
 *  เช่น notEnabledMessage('ประวัติเงินเข้า-ออก', CONTACT.finance) */
export function notEnabledMessage(featureLabel, contact = CONTACT.admin) {
  return `ระบบ${featureLabel}ยังไม่ถูกเปิดใช้งาน กรุณาแจ้ง${contact}`;
}

/** รายละเอียดทางเทคนิคสำหรับคนที่แก้ได้ — ลงที่ console ไม่ใช่บนหน้าจอผู้ใช้
 *  เรียกตอนจับ error ได้ คู่กับ notEnabledMessage ที่ส่งให้หน้าจอ */
export function logSetupHint(featureLabel, migrationFile) {
  console.error(
    `[setup] ${featureLabel} ยังใช้ไม่ได้ — ยังไม่ได้รัน supabase/migrations/${migrationFile} บนฐานข้อมูล`
  );
}
